# Serabut PXE Boot Server Configuration

# Interface to bind to (default: 0.0.0.0 for all interfaces)
interface=0.0.0.0

# Port to listen on (default: 4123)
port=4123

# Log level: error, warn, info, debug (default: info)
log_level=info

# Path to config data (default: /var/lib/serabut/config)
# config_path=/var/lib/serabut/config
